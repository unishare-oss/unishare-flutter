# Session: 2026-05-07-save-post

**Date:** 2026-05-07  
**Member:** Nang Hayman Aye Mya  
**Agent:** flutter-engineer  
**Task:** Implement SPEC-0007 Save Post feature

## Context

PROP-0007 and SPEC-0007 are both APPROVED. The spec defines a hybrid bookmarking system:
- Hive for guest users (local-only, no Firestore writes)
- Firestore subcollection `users/{uid}/savedPosts/{postId}` for authenticated users
- Silent merge-on-login (guest saves → Firestore on auth transition)
- `SaveButton` is an amber bookmark icon placed top-right of the PostCard header row
- `SavedPostCard` layout confirmed from Figma: type badge + courseId + bookmark icon, title, author, commentsCount + time
- Empty state: text-only "No saved posts yet." — no illustration
- Guest banner: "Saved posts are stored locally..." + "→ Sign in to sync" link

## Plan

1. Domain layer — entities, repository interface, 5 use cases ✅ Done
2. Data layer — Hive model + adapter, Freezed DTO, 2 datasources, 2 repository impls ✅ Done
3. Core storage — `saved_post_box.dart` + `initSavedPostBox()` ✅ Done
4. Presentation — 3 providers, SavedPostCard, SaveButton, SavedScreen ✅ Done
5. Modify `main.dart` — add `initSavedPostBox()` call ✅ Done
6. Modify `PostCard` — add SaveButton (top-right of header row) ✅ Done
7. Modify `PostDetailScreen` — wire SaveButton + savesCount label ✅ Done
8. Modify `router.dart` — guest shell with FEED | SAVED | SIGN IN tabs ✅ Done
9. Run `dart run build_runner build` ✅ Done — 57 outputs
10. Run `flutter analyze` ✅ Clean — 0 issues

## Notes

- `abstract class SavedPostDto` required (Freezed v3 in this project always needs `abstract`)
- `AsyncValue.valueOrNull` not available in flutter_riverpod 3.3.1 — use `.asData?.value` instead
- `Post` entity has no `commentsCount` field — `SavedPostSnapshot.commentsCount` defaults to 0 for all newly saved posts
- Router guest shell (FEED | SAVED | SIGN IN) requires restructuring the `StatefulShellRoute` — the current shell has 4 branches; guest needs a separate 2-branch shell with SIGN IN as an action in the AppBar or scaffold
- PostDetailScreen save button: the Figma shows a "1 save" count label next to the bookmark icon; `Post.savesCount` does not exist on the entity — the flutter-engineer must decide whether to add the field to `Post` or use a separate Firestore read

## Handoff

**To:** architect or qa-engineer (reviewer)  
**Done:** All domain + data + core storage + presentation layers implemented and passing `flutter analyze`. PostCard save button wired. SavedScreen fully implemented with guest banner, empty state, skeleton, error state.  
**Not done:** PostDetailScreen save button wiring, router guest-shell restructure (`router.dart`).  
**Watch out for:** typeId 2 for `SavedPostHiveModelAdapter` — confirmed unique. The router guest-shell change is non-trivial and should be reviewed before merging. `commentsCount` on `SavedPostSnapshot` will always be 0 until `Post.commentsCount` is added (deferred from SPEC-0006).
