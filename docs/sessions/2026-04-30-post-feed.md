# Session: 2026-04-30-post-feed

**Date:** 2026-04-30  
**Member:** [name]  
**Agent:** architect  
**Task:** Implement post-feed feature

## Context

<!-- What the previous agent or human handed off. Link to relevant decision records or agent-run reports. -->

New feature scaffold. No prior agent session. See `apps/mobile/lib/features/post_feed/` for the empty
directory structure.

## Plan

1. Define domain entities (`Post`, `Author`) — pure Dart, zero Flutter/Firebase imports
2. Define repository interface in `domain/repositories/`
3. Decide Firestore collection schema (`posts/{postId}`) and write a Tech Spec
4. Implement Firestore data source and Freezed DTO models
5. Implement repository adapter in `data/repositories/`
6. Write use cases: `GetPostFeed`, `CreatePost`, `DeletePost` (one class, one public method each)
7. Wire Riverpod providers (`@riverpod` code gen) in `presentation/providers/`
8. Build `PostFeedScreen` and register route in GoRouter
9. Add feature widgets (`PostCard`, `PostFeedList`)
10. Write widget tests for every screen

### Domain Entities Needed

- `Post` — id, authorId, title, body, mediaUrls, tags, createdAt, updatedAt, likesCount
- `Author` — id, displayName, avatarUrl

### Firestore Collections

- `posts/{postId}` — top-level collection, indexed by `createdAt` descending for feed
- `posts/{postId}/likes/{userId}` — subcollection for like tracking (no double-counting)

### Screens Required

- `PostFeedScreen` — paginated feed list
- `PostDetailScreen` — single post view with comments entry point

## Notes

<!-- Running notes during the session — discoveries, blockers, pivots. -->

## Handoff

**To:** flutter-engineer  
**Done:** folder scaffold created, session plan drafted  
**Not done:** all implementation  
**Watch out for:** Feed uses page-number pagination at the domain layer; the data source caches a
`DocumentSnapshot` per page boundary internally — Firestore has no native offset. Tech Spec goes
at `tech-specs/0003-post-feed.md`. Resolve the 4 open questions in the proposal before writing it.
