# Session: 2026-05-09-requests

**Date:** 2026-05-09  
**Member:** Nang Hayman Aye Mya  
**Agent:** flutter-engineer  
**Task:** Implement the Requests feature (SPEC-0008)

## Context

- Proposal: [PROP-0008](../../tech-proposals/0008-request-post.md) — APPROVED
- Spec: [SPEC-0008](../../tech-specs/0008-request-post.md) — DRAFT (ready for implementation)
- Scaffold created: `apps/mobile/lib/features/requests/` folder structure is in place
- Existing placeholder: `presentation/screens/requests_screen.dart` must be replaced (not deleted)
- Router: `/more/requests` route already exists; add `/more/requests/:requestId` for the detail screen
- UI reference: screenshots at `tech-proposals/Screenshot 2026-05-09 231555.png` (list), `231606.png` (new request dialog), `231616.png` (detail), `231630.png` (suggest fulfillment dialog)

## Plan

1. **Domain layer** — define `ContentRequest` entity + `RequestStatus` enum, `Suggestion` entity, `RequestRepository` abstract interface, and all 5 use cases (`WatchRequests`, `CreateRequest`, `WatchSuggestions`, `SuggestFulfillment`, `ToggleUpvoteRequest`). Zero Flutter/Firebase imports.
2. **Data layer** — implement `RequestDto` and `SuggestionDto` (Freezed + JSON), `RequestFirestoreDatasource` (Firestore streams + writes), `RequestRepositoryImpl`.
3. **Presentation layer** — Riverpod providers (`requestsProvider` stream, `suggestionsProvider`, `upvoteProvider`), then screens and widgets in this order: `RequestCard` → `RequestFilterBar` → `UpvoteButton` → `RequestsScreen` (replaces placeholder) → `NewRequestDialog` → `RequestDetailScreen` → `SuggestionCard` → `SuggestFulfillmentDialog`.
4. **Router** — add `GoRoute(path: 'requests/:requestId', ...)` under the `/more` branch.
5. **Firestore rules + indexes** — add rules for `requests/*`, `requests/*/suggestions`, `requests/*/upvotes`; add two composite indexes.
6. **Tests** — widget tests for all screens and dialogs; unit tests for all use cases and DTOs.

## Notes

<!-- Running notes during the session — discoveries, blockers, pivots. -->

## Handoff

**To:** qa-engineer (test review) + architect (spec compliance check)  
**Done:**  
**Not done:**  
**Watch out for:**
- `CourseStep` widget (`features/post/presentation/widgets/course_step.dart`) takes `universityId`, `selectedDepartmentId`, `selectedYear`, `selectedCourseId` — wire up correctly in `NewRequestDialog`
- `fulfilledByPostId` / `fulfilledByPostTitle` are denormalized on the request doc for fast list rendering — set them inside `suggestFulfillment` when it's the first suggestion
- `upvoteCount` is a denormalized int — use a Firestore `FieldValue.increment(±1)` transaction alongside the upvotes subcollection write
- Status open question: spec assumes `status` → `fulfilled` on first suggestion automatically; confirm with team before writing the use case
