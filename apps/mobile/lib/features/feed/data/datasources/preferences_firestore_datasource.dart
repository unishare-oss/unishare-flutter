// TODO: implement — read and write users/{uid}/preferences document
// Confirm whether preferences is a sub-document or top-level field on users/{uid} before implementing
// Use merge-write semantics (SetOptions(merge: true)) to preserve unrelated fields
